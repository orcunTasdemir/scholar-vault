# Backend Environment Variables
# Copy this file to .env and fill in your values

# Database Configuration
# For local development (requires local PostgreSQL):
DATABASE_URL=postgresql://your_username@10.0.0.57/scholarvault #was localhost

# For Docker development, this is set automatically in docker-compose.yml:
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/scholarvault

# JWT Secret Key
# Use a strong random string in production
# Generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this